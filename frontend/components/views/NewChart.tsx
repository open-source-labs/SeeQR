import React from 'react';
// import { AppState } from '../../../types';

const NewChart = ({ show }) => {
  if (!show) return null;
  return (
    <div>
      <h1>NEW PAGE</h1>
    </div>
  );
};

export default NewChart;
